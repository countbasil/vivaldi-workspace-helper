<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Keeps relay/server.js always running so the "jump to last auto-routed tab"
  hotkey has something to talk to. Install once:

    cp relay/com.aaron.vivaldi-workspace-relay.plist ~/Library/LaunchAgents/
    launchctl load ~/Library/LaunchAgents/com.aaron.vivaldi-workspace-relay.plist

  After editing this file or server.js, reload with:

    launchctl unload ~/Library/LaunchAgents/com.aaron.vivaldi-workspace-relay.plist
    launchctl load ~/Library/LaunchAgents/com.aaron.vivaldi-workspace-relay.plist
-->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.aaron.vivaldi-workspace-relay</string>
    <key>ProgramArguments</key>
    <array>
        <string>/opt/homebrew/bin/node</string>
        <string>/Users/aaron/Developer/vivaldi-tools/vivaldi-workspace-helper/relay/server.js</string>
    </array>
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>
    <key>StandardOutPath</key>
    <string>/Users/aaron/Library/Logs/vivaldi-workspace-relay.log</string>
    <key>StandardErrorPath</key>
    <string>/Users/aaron/Library/Logs/vivaldi-workspace-relay.log</string>
</dict>
</plist>
